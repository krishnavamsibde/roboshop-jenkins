- name: template
  template:
    src: pipeline.xml
    dest: /tmp/pipeline.xml
  loop:
    - { repoName: "cart", componentName: "cart" }


- name: Create a jenkins job using the token
  community.general.jenkins_job:
    config: "{{ lookup('template', 'templates/pipeline.xml') }}"
    name: test
    url: http://jenkins.kvdevops.site:8080
    user: admin
    token: 114bbdbb72d37a4594148869f959450bd7
